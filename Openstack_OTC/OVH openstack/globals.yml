---
workaround_ansible_issue_8743: yes
kolla_base_distro: "ubuntu"

# All traffic goes through enp1s0f0 (single NIC with IP aliasing)
network_interface: "enp1s0f0"
api_interface: "enp1s0f0"
tunnel_interface: "enp1s0f0"
storage_interface: "enp1s0f0"

# Use the second IP for VIP (where OpenStack APIs will listen)
kolla_internal_vip_address: "51.38.118.220"
kolla_external_vip_address: "51.38.118.220"

# Single node - disable HA components
enable_haproxy: "no"
enable_keepalived: "no"
enable_proxysql: "no"

# Core OpenStack services
enable_openstack_core: "yes"
neutron_plugin_agent: "openvswitch"

# For single NIC setup with provider network
# Use VLAN or flat networking on the same interface
neutron_external_interface: ""  # Leave empty for provider network on main interface

# Network type configuration
# For single NIC, use provider networks on the same interface
enable_neutron_provider_networks: "yes"

# Cinder block storage
enable_cinder: "yes"
enable_cinder_backend_lvm: "yes"
cinder_volume_group: "cinder-volumes"
